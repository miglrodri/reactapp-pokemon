import React, { Component } from 'react';

class Pokemons extends Component {
    render() {
        return (
            <div>
                print a table with a list of pokemons.
                create a function and call it inside component did mount that will fetch() the api
                url: https://pokeapi.co/api/v2/pokemon
            </div>
        );
    }
}

export default Pokemons;